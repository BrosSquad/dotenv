# This is Comments

# all empty lines will be skipped

APP_NAME = Test
DB_NAME = test

NAME AND VAlUE = something
# Test is Interpolated
INTERPOLATION = "${APP_NAME} is Interpolated"

MULTI_LINE =    " this
              is multi line
"

COMMENTS_AT_END_OF_VALUE = Name #here is some comment
